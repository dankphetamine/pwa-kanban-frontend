export const Colors = {
	bgColor: { light: 'messenger.200', dark: 'blue.800' },
	color: { light: 'black', dark: 'white' },
};

const port = 4000;
export const gqlUrl = `http://localhost:${port}/graphql`;
