query Project($id: Int!) {
	project(id: $id) {
		...BaseProject
		tasks {
			id
			title
			description
			status
		}
	}
}

query Projects($filter: ProjectFilterInput!) {
	projects(filter: $filter) {
		...BaseProject
		owner {
			id
			name
			image
		}
		tasks {
			id
			title
		}
	}
}
