mutation Login($input: AuthInput!) {
	login(input: $input) {
		...BaseUser
		name
	}
}

mutation LogOut {
	logout
}

mutation Register($input: AuthInput!) {
	register(input: $input) {
		...BaseUser
	}
}
